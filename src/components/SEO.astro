---
import { site } from '../data/site';

interface Props {
  title: string;
  description: string;
  image?: string;
  /** When both are set, emit accurate og:image width/height. Otherwise omit them. */
  imageWidth?: number;
  imageHeight?: number;
  canonical?: string;
  type?: 'website' | 'article' | 'profile';
  noindex?: boolean;
  schema?: object;
}

const {
  title,
  description,
  image = '/images/og-default.jpg',
  imageWidth,
  imageHeight,
  canonical,
  type = 'website',
  noindex = false,
  schema,
} = Astro.props;

const base = Astro.site ?? new URL(site.url);
const canonicalUrl = canonical ? new URL(canonical, base).toString() : new URL(Astro.url.pathname, base).toString();
const imageUrl = new URL(image, base).toString();
const fullTitle = `${title} | ${site.name}`;
const resolvedWidth =
  typeof imageWidth === 'number' && imageWidth > 0
    ? imageWidth
    : image === '/images/og-default.jpg'
      ? 1200
      : undefined;
const resolvedHeight =
  typeof imageHeight === 'number' && imageHeight > 0
    ? imageHeight
    : image === '/images/og-default.jpg'
      ? 630
      : undefined;
const hasImageDimensions =
  typeof resolvedWidth === 'number' && typeof resolvedHeight === 'number';
---

<title>{fullTitle}</title>
<meta name="description" content={description} />
<link rel="canonical" href={canonicalUrl} />

<meta name="author" content={site.advisor} />
<meta name="theme-color" content="#010E71" />
{noindex && <meta name="robots" content="noindex, nofollow" />}
{!noindex && <meta name="robots" content="index, follow, max-image-preview:large" />}

<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={imageUrl} />
{hasImageDimensions && <meta property="og:image:width" content={String(resolvedWidth)} />}
{hasImageDimensions && <meta property="og:image:height" content={String(resolvedHeight)} />}
<meta property="og:image:alt" content={`${site.name} — ${site.tagline}`} />
<meta property="og:site_name" content={site.name} />
<meta property="og:locale" content="en_US" />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={imageUrl} />

{schema && <script type="application/ld+json" is:inline set:html={JSON.stringify(schema)} />}
